- name: default
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: PG_DATABASE_URL
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include default/tables/tables.yaml"
# 本番用データベース（ローカル開発時はコメントアウト）
# - name: travel-planner-prd
#   kind: postgres
#   configuration:
#     connection_info:
#       database_url:
#         from_env: PG_DATABASE_URL
#       isolation_level: read-committed
#       use_prepared_statements: false
#   tables: "!include travel-planner-prd/tables/tables.yaml"
